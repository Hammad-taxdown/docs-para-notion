# Arranque · 17/08/2026

> El prompt que se pega al empezar la sesión. Copiar de `Retomamos Beckham` hasta el final.

---

```
Retomamos Beckham. Lee .spartax/log.md (las entradas del 16/08, que son cinco bloques),
state.json y .spartax/context.md antes de proponer nada. La sección «Estado al 16/08/2026»
del context.md es el resumen corto.

## Dónde lo dejamos el 16/08

**NADA DEL BOT SE TOCÓ.** La sesión entera fue documentación, orden y metadatos.
`beckham_bot` sigue en `d15a8da8` con 54 nodos, `beckham_generar_030` en `b9653e09` e
`beckham_informe_mobility` en `fbac1c39`. Los tres auditados por MCP y sin deriva.

- **`docs/arquitectura-completa-2026-08-16.md`** — la arquitectura de punta a punta, con
  5 diagramas Mermaid y las 14 decisiones de arquitectura con su porqué. Los 5 pasan
  `mermaid.parse()` de Mermaid v11, que es el parser que usa GitHub.
- **`proyecto-mobility/`** — la carpeta para el repo nuevo (la crea él, no yo): README de
  894 líneas con banner y 7 diagramas en PNG, sección con los 93 nodos explicados uno a
  uno, y `workflows-n8n/` con **los 7 workflows exportados, la primera vez que están
  todos**. `beckham_adjuntos_huerfanos` no tenía export hasta ese día.
- **La carpeta de trabajo, reorganizada**: de 34 ficheros sueltos en la raíz a 1.
  `push-cierre.sh` actualizado, porque copiaba de la raíz por nombre. Push hecho.
- **Airtable: las 93 columnas de `Empleados` ya tienen descripción.** Las 60 que faltaban
  se escribieron por MCP, cada una con su grupo delante (`① IDENTIDAD`, `② PERSONALES`…).

### Tres correcciones a la documentación, y las tres las cazó él

1. **La tabla tiene 93 columnas, no 64.** Estaba mal en tres sitios.
2. **NO es cierto que «no haya tests».** Hay **14 ficheros de prueba y 3.609 líneas**, y
   `montar-nodo-informe.sh` los usa **como puerta**: `exit 1` si alguna está roja y
   revierte al `COMPLETO` anterior si falla la de integración. La frase venía arrastrada
   del briefing del 12/08, cierta ANTES de que existieran los generadores.
3. **Campo nuevo = CUATRO sitios**, no tres: tool, validador, mapeo de Airtable Y prompt.

### Límite nuevo y duro

**Airtable NO deja reordenar columnas por API.** El orden de campos es propiedad DE LA
VISTA y la Metadata API no expone endpoint: `create_field` añade siempre al final,
`update_field` solo toca nombre/descripción/fórmula, y no hay `create_view`. Se arrastra a
mano en la UI. El orden propuesto está en `docs/orden-columnas-empleados-2026-08-16.md`.
**Él hizo la mitad el 16/08.**

### Sin respuesta, y no se ha tocado

`PaisNacimiento`, `Nacionalidad` y `UltimoPaisResidencia` llevan la lista entera de 245
países volcada DENTRO del campo descripción, lo que deja el popup de información
inservible. Se preguntó si limpiarlas y no contestó.

## Hoy toca, en este orden

1. **Probar `FechaLlamada` en una conversación real.** Es lo único del 15/08 que no llegó
   a empezar, y lleva dos sesiones aplazándose. Decirle al bot una fecha de reunión, ver
   que llega a la columna `FechaLlamada`, marcar `RegenerarInforme` y comprobar que el PDF
   la imprime en «Fecha de la reunión» en vez de «Por confirmar». Los cuatro sitios ya
   están puestos (parámetro 41 de la tool, línea 222 del validador, columna 57 del Upser y
   el prompt v8): **si llega vacía, el que falla es el prompt.**
   La prueba SOLO vale escribiendo desde el Messenger como cliente. Desde el Inbox es un
   mensaje de admin y no dispara nada.

2. **WP-220: el corpus fiscal al prompt.** Desbloquea el FAQ (`WP-221`), cuyo gate son las **30 preguntas doradas**.
   Ya extraído en `docs/corpus-fiscal-beckham-2026-08-13.md` y el umbral no bloquea.
   **Ojo: falta meter que la prestación por paternidad de la SS SÍ tributa**, que la
   plantilla del informe ya lo dice y el prompt no.

3. Si queda hueco: la propuesta de quitar el pegado manual de los nodos de código
   (`docs/propuesta-quitar-el-pegado-manual-2026-08-14.md`). **Está fuera del alcance del
   31/08** y solo entra si él lo dice; medio día, y quita el pegado de 239 KB por cambio.

## Antes de nada

Auditoría por MCP de los tres workflows: `beckham_bot` (nhOwpiGxikeU5DLR),
`beckham_generar_030` (OoJ2l7PmxSHLxXA4) y `beckham_informe_mobility` (Us5sFgXD9qVxJvxO).
`get_workflow_details` revienta el límite de lectura y el harness guarda la respuesta en un
fichero: se audita con jq o python desde bash, sin gastar contexto. Y el MCP devuelve
`credentials={}` en TODOS los nodos, también en los que funcionan: si hay que comprobar una
credencial, se ejecuta el workflow.

## Recordatorios

- **Tiene una pregunta importante pendiente de hacerme** y quiere que se la recuerde.
- El push va **una vez, al cerrar**. Si ya lo ha hecho, no se vuelve a mencionar.
```
