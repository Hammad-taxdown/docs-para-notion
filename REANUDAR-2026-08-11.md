# Prompt para reanudar · martes 11/08/2026

Pégalo tal cual al abrir la sesión.

---

```
/spartax Retomamos el proyecto Beckham. Lee .spartax/log.md (las entradas del 10/08),
state.json y .spartax/context.md antes de proponer nada.

Contexto de dónde lo dejamos el lunes 10:
- beckham_bot está en versionId 7e499c3b, 51 nodos, 52 columnas, tool de 36 parámetros,
  prompt v7 con tag prod. Cero .item, typecast en true.
- El auth de los webhooks está DESACTIVADO a propósito en los cuatro nodos, porque con la
  credencial puesta la API de n8n no puede leer el workflow y se pierde el diff estático.
  La credencial beckham_webhook_auth sigue creada.

Hoy toca, en este orden:

1. Cerrar las tres que quedaron a medio:
   a) Una conversación completa en el Messenger que llegue al final, para probar que la
      conversación de Intercom SE CIERRA de verdad. El mecanismo está publicado y probado
      por curl, pero eso solo prueba que el dato se guarda, no que se cierre. Hay que
      llegar hasta el "¿te queda alguna duda?" y contestar que no.
   b) T050: necesito de ti la lista de casillas de los Modelos 030 y 149 y de qué columna
      sale cada dato. Es lo único que bloquea T036.
   c) T053: reactivar el auth en los cuatro nodos (los dos webhooks y las dos tools) y
      probarlo con una conversación. OJO: si las tools se quedan sin la credencial, el bot
      deja de guardar y los curls no lo detectan. Hazlo antes de terminar, no como último
      clic.

2. Empezar a subir a producción. Es el tramo 10-14/08 del plan maestro: WP-207, WP-208,
   WP-209 y la cadena del modo (WP-210 a WP-213), más WP-216 y el bloque 7 (T015) con los
   cuatro puntos D/H/G/N.

3. Después de producción y su testeo, el FAQ (17-21/08): WP-220, WP-221, WP-229. Recuerda
   que el FAQ es una rama HERMANA de los filtros, no va dentro de F1-F3.

Los 10 paquetes que quedan: T015, T005, T006, T007, T008, T041, T036, T057, T031, T052.

Antes de tocar nada, haz la auditoría previa por MCP de beckham_bot y dime si alguien ha
cambiado algo desde el 10/08 a las 17:48.
```

---

## Chuletas para mí (el asistente), no hace falta pegarlas

**Lo que puede morder mañana:**

- **La credencial del MCP.** Si los devs comparten `chTgEmF0KkSvcivT` con la identidad del servidor
  MCP integrado de n8n, el auth se puede dejar puesto sin perder el diff. Si no, hay que elegir entre
  el 403 y la auditoría. **No proponer crear un API key personal: no existe esa opción**, el MCP es
  el servidor integrado (`es.synapse.rentax.es/mcp-server/http`).
- **No arrastrar parches sin validar a una publicación de prompt.** El v5 iba sin probar, entró
  dentro del v6 y metió un bucle infinito en la pregunta del idioma.
- **En un `If`, decir siempre qué rama por su significado, no por el número.** `out0` de
  `¿Prompt vacio?` es la rama del ERROR, porque la condición está escrita en positivo.
- **`Callback_Intercom` y `Validar y Normalizar` son ejecuciones distintas.** Conversación vs
  escritor. No se pueden referenciar entre sí.
- **Al pegar un bloque grande en un nodo de código**, contar las declaraciones clave para descartar
  duplicados: un `const` duplicado no impide publicar, revienta en ejecución.
- **En cualquier cambio del nodo de Airtable**, comparar la **lista** de nombres de columna contra la
  versión previa, nunca el número.
- **Borrar las filas de prueba también de los curls de humo**, no solo de las pruebas con datos.

**Verificaciones pendientes, declaradas:**

- El aviso de discrepancia de fecha **no se ha probado en conversación real**: hace falta subir un
  documento cuya fecha no cuadre con la declarada.
- El cierre de Intercom **no se ha probado desde el flujo**, solo el POST suelto.
- El campo `texto` de la Data Table del respaldo: **el MCP no puede leer filas de una Data Table**,
  así que confirmar en la UI que tiene ~46.000 caracteres (el v7) y no los 37.097 del v3.

**Deuda que hay que recordarle:**

- El token de los webhooks ha pasado por la terminal y está en el log. **Generar otro antes de
  producción.**
- Si el cliente nunca contesta a "¿alguna otra duda?", la conversación se queda abierta para
  siempre. Haría falta un proceso por tiempo.
- Comentario obsoleto en las líneas 338-339 de `Validar y Normalizar`: dice que hay dos opciones
  duplicadas en `Propiedades` e `Inversiones`, y ya no las hay.
- `Empleados-Grid view*.csv` tiene PII real y **sigue en el historial de git**; el repo fue público
  del 3 al 5/08. Limpiar el historial es decisión pendiente del usuario.
