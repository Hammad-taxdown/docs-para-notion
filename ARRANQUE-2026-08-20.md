# Arranque · 20/08/2026

Retomamos Beckham. Lee `.spartax/log.md` (las **ocho** entradas del 19/08), `state.json` y
`.spartax/context.md` antes de proponer nada. El `CLAUDE.md` se reescribió ayer en seis sitios: la
escalera de Status, el lector, el umbral y cuatro reglas con prueba nuevas.

## Antes de nada · la auditoría

Los tres por MCP: `beckham_bot` (`nhOwpiGxikeU5DLR`), `beckham_generar_030` (`OoJ2l7PmxSHLxXA4`),
`beckham_informe_mobility` (`Us5sFgXD9qVxJvxO`). `get_workflow_details` revienta el límite de lectura
y el harness guarda la respuesta en un fichero: se audita con `jq` o `python` desde bash. Y el MCP
devuelve `credentials={}` en **todos** los nodos, también en los que funcionan.

**Los tres versionId cambiaron ayer.** Deben estar en `ce6e486b` (bot, **55 nodos**), `94b353cd`
(`.030`) y `74aa294c` (informe), los tres con `versionId == activeVersionId` y con
`errorWorkflow = BJfExmwu1fI1aPpY`. Si alguno difiere, alguien tocó algo después de mí.

## Lo que se hizo ayer · 14 cambios, todos pegados y verificados

**La escalera de Status, rehecha.** El bot escribe el **3** (`Decidir_Status`) y el informe escribe el
**4** (`Marcar InformeListo`). Los dos generadores filtran `OR(Status=3, Status=4)`, y de la rama de
la automatización `3b` de Airtable se quitó el `3`. **Son cinco sitios, no uno.**

El resto: validador de **73.081 car** con **729 gentilicios** (de resolver 97 países de 245 a **241**),
umbral a **50.000**, estado civil a **tres**, el 1 de julio fuera del enrutado, la tool de 41 a **40**
parámetros, el lector de 21 a **47 claves** con la whitelist vaciada, **prompt v10** (60.328 car, tag
`prod`), el intercambio de las automatizaciones `2` ↔ `2b`, la guarda de adjuntos de `3b`, y el
**informe PDF reducido a nombre + apellidos + fecha de alta** (`COMPLETO` de 241.272 car).

**Siete puertas en verde**, cero rojas: `test-decidir-status.js` 15 · `test-validador-2026-08-19.js`
31 · `test-prompt-v10.js` 35 · `test-lector-expediente.js` 14 · `test-informe-datos.js` 226 ·
`test-informe-cuerpo.js` · y los dos scripts de montaje en `exit 0`.

Los dos repos subidos: `docs-para-notion` (38 ficheros) y `proyecto-mobility-2026` (`81c5763`, con
los tres workflows reexportados).

## Hoy toca, en este orden

### 1 · UNA CONVERSACIÓN. Cierra dos cosas de golpe

El prompt v10 **no se puede verificar ni por MCP ni por API**: solo leyendo la traza de una ejecución
del agente. Y desde que se publicó el bot ayer a las 14:11 hay **cero ejecuciones**.

Esa misma traza cierra **`WP-204`**, que ya cumple **4 de sus 5 criterios medidos** (falta solo que el
prompt se vea resuelto en el log, sin `{{current_date}}`).

Tres cosas que mirar en la conversación:

- que **no** pregunte la fecha de la llamada al cerrar un caso complejo;
- que llegando **después del 1 de julio** no enrute a llamada por eso;
- que con **52.000** trate el caso como «al límite» y no lo descarte.

### 2 · LA PRUEBA DEL DISPARADOR. La que nunca se ha hecho

**Ningún tick de 15 minutos ha producido nada nunca.** Las cinco generaciones que existen son
`mode=manual` lanzadas desde la UI. Así que:

1. Cerrar una conversación con «expediente completo» → la fila queda en **3**.
2. **No tocar nada.** No lanzar los workflows a mano. **No marcar `EnviarBorradores`.**
3. En ≤15 min: `Fichero030` con un `.030` de 2.700 bytes, `InformePdf` con el PDF, `InformeListo`
   marcado y el `Status` en **4**.
4. En n8n, que las dos ejecuciones sean **`mode=trigger`**, no `manual`.

### 3 · PROD

Lo que lo bloquea: `WP-216` (recolocar D/H/G/N) y `T052` (idioma al canvas), que son **canvas de
Intercom** y los haces tú; limpiar las **5 filas de prueba**; y el auth con token nuevo
(`WP-203`/`T053`), que es **lo último de todo** por decisión tuya.

## Sin decidir, y son tuyas

1. **¿`DiscrepanciaFechaAlta` debe forzar el cierre a `Llamada agendada`?** La línea 322 del prompt
   dice que una discrepancia de fecha de alta es motivo de **llamada** aunque el caso sea claro, y
   `Decidir_Status` **no mira esa columna**. Pasó en la ejecución `8118002`.
2. **`T059`, segunda mitad: los PDF adicionales.** La primera mitad está hecha. Falta que me pases
   cómo son los nuevos.

## Lo que queda abierto y no bloquea

- **`assets/diagramas/07-escalera-status.png`** sigue mostrando el salto del 2 al 4. Dijiste que lo
  rehacías con Claude Design. Hay un aviso en el README encima del propio diagrama.
- Los **tres satélites** (`beckham_f2_plazo.`, `beckham_analizar_documento`,
  `beckham_adjuntos_huerfanos`) siguen **sin `errorWorkflow`**.
- **`beckham_f2_plazo.`** está activo y recibiendo webhooks pero **ningún nodo del bot lo llama**.
- La columna **`FechaLlamada`** queda huérfana a propósito. Bórrala tú cuando quieras.
- La automatización **`Crear Check out`** sigue `undeployed`.
- **Tres de los cuatro formularios** no tienen automatización detrás.
- El **camino crítico** son 21 puntos y su primer eslabón sin cerrar sigue siendo **`WP-207`**.
  Tracker: 39 WPs, **11 cerrados, 28 abiertos, 51 puntos**.

## Trampas frescas de ayer, con cicatriz

- **`montar-nodo-informe.sh` revierte el `COMPLETO` si falla la de integración, y la de integración
  lee el `COMPLETO` del disco.** Si cambias una pieza *y* su prueba a la vez, la primera pasada
  revierte y la prueba sigue midiendo el nodo viejo: parece que tu cambio no ha entrado. Desde ayer
  el candidato rechazado se guarda en `.rechazado`.
- **Un extractor de PDF que no encuentra streams devuelve falsos verdes en cascada.** Este PDF **no
  lleva streams comprimidos**: el texto va en operadores `Tj`. El extractor bueno es el de
  `test-informe-integracion.js`.
- **Una prueba que ensucia un marcador que ya no se imprime no prueba nada.** Al quitar un campo del
  documento, mira qué pruebas lo usaban **como vehículo** y no como objeto.
- **El `1415` y el `1406` del `.030` son el MISMO byte** (bloque A vs fichero; `1415 − 1406 = 9`). No
  «corrijas» ninguno de los dos.
- **`AnioDesplazamiento` y `FechaAlta` están en `error` en las cuatro filas y NO PASA NADA.** El
  informe no los usa. No lo vuelvas a abrir.
- **Iciar también prueba el bot.** Un estado raro en Airtable es otra persona antes que un bug.
