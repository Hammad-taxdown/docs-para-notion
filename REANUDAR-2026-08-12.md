# Prompt para reanudar · miércoles 12/08/2026 (o sesión de noche del 11)

Pégalo tal cual al abrir la sesión.

---

```
/spartax Retomamos Beckham. Lee .spartax/log.md (las entradas del 11/08), state.json y
.spartax/context.md antes de proponer nada.

Dónde lo dejamos el 11:
- beckham_bot en versionId d09044fe, 51 nodos, 52 columnas, tool de 36 parámetros,
  prompt v7 con tag prod. Cero .item, typecast en true. El auth sigue OFF a propósito
  y es lo ÚLTIMO que se hace, justo antes de publicar.
- Se publicaron dos parches y NO están probados en conversación, por decisión mía:
  WP-238 (el Status depende de motivo_cierre) y WP-239 (ResumenBot con ficha + prosa).
- El circuito del modelo 030 quedó fijado: el agente monta el fichero .030 desde
  plantilla y lo deja adjunto en Airtable; el resto del camino es humano.

Hoy toca, en este orden:

1. Una conversación completa que pruebe de una vez los dos parches sin verificar:
   que un expediente completo deja Status en "3. Pte hacer informe" y que ResumenBot
   trae la ficha de 18 etiquetas y debajo el párrafo.

2. WP-235: el generador del fichero .030. Antes de escribir código necesito de ti
   una SEGUNDA muestra .030 con datos distintos, mejor de alguien con DOS apellidos
   y de otra provincia — con una sola muestra los anchos de apellidos y nombre son
   inferencia y no quiero construir sobre eso.

3. Los dos huecos que sí obligan a tocar Airtable: primer/segundo apellido y
   municipio de residencia con su código INE.

4. WP-237: solo falta el salto de Status 7 → 8 cuando el cliente confirma. Necesito
   que me digas si un "4.3 Confirmo el 030 pero necesito modificar el 149" cuenta
   como confirmado, ahora que el 149 lo hacen los fiscales a mano.

Antes de tocar nada, auditoría previa por MCP de beckham_bot y dime si alguien ha
cambiado algo desde el 11/08 a las 13:21.
```

---

## Chuletas para mí (el asistente), no hace falta pegarlas

### Decisiones que el usuario cerró el 11/08 — no reabrir

- **Solo el 030.** El 149 lo preparan los fiscales a mano.
- **El bot no toca la AEAT y no presenta nada.** Genera el `.030` y lo deja en Airtable.
- **El cónyuge NO se rellena** (apartado 3 del modelo, 301–317). Se cayeron 17 campos.
- **Las causas son 107, 103 y 105**, constantes.
- **`{{paisOrigen}}` del informe Mobility = `Nacionalidad`**, no país de nacimiento.
- **El auth de los webhooks es lo último**, justo antes de publicar. Token nuevo entonces.
- **U2 autorizada** y su premisa caducó: la conversación sonda ya no crea ticket ni manda correo.
- **El canvas es implementación directa en producción**, no maqueta.

### Lo que puede morder

- **El fichero `.030` es posicional, no XML.** Contrato en `docs/contrato-fichero-030-2026-08-11.md`.
  Los anchos de **apellidos y nombre son inferencia** con una sola muestra: no construir sin la segunda.
- **La lógica de la casilla 217 YA existe** dentro de la fórmula `Situación fiscal Anio Desplazamiento`
  («183 días / 1 de julio»). Reutilizarla. Escribir una segunda es garantizar que se contradigan.
- **`WP-220-corpus-fiscal-buscar-contexto.md` contradice al mapa**: sigue en `skeleton`, bloqueado por
  M4 y con una tool de búsqueda que el mapa ya descartó. El corpus está aprobado por Alina desde el
  11/08. **Hay que reescribir el `.md` o borrarlo.**
- **`map.html` NO se regenera desde el frontmatter.** Es un array JS a mano con notas largas.
  **Ejecutar `/prd:map` lo destruiría.** Backups: `map.html.bak-20260811`.
- **La escalera de `Status` solo sube.** Por eso el bot y las automatizaciones de Airtable conviven.
  Y por eso un expediente ya cerrado en `2` no se arregla solo con el parche de hoy.
- **Peldaños 4, 5 y 6 del `Status` huérfanos**: no los escribe nadie.
- **`AnioDesplazamiento` es `aiText`, no fórmula**, y se ha visto en `state:"error"`.

### Verificaciones declaradas pendientes

- **WP-238 y WP-239: publicadas y sin probar.** Es lo primero de mañana.
- **El aviso de discrepancia de fecha de alta** sigue sin probarse: hace falta subir un documento
  cuya fecha no cuadre con la declarada.
- **Cuarta marca de la posición 172** del `.030` sin identificar (hipótesis: casilla 201).
- **Cabecera `20250203`** del `.030` sin identificar.

### Deuda

- **PII, sin resolver:** un borrador `.pdf` del 030 nombrado como del usuario contenía el NIE, el
  apellido y el nombre de **otra persona real**. Falta saber si fue fichero equivocado o un incidente
  del generador. **Si es lo segundo, es de hoy, no del 31.**
- `Empleados-Grid view*.csv` con PII real **sigue en el historial de git**; el repo fue público del
  3 al 5/08.
- Si el cliente nunca contesta al «¿alguna otra duda?», la conversación **se queda abierta para
  siempre**. Haría falta un proceso por tiempo.
- Comentario obsoleto en las líneas 338-339 de `Validar y Normalizar`.
- El token de los webhooks pasó por la terminal: **generar otro antes de producción**.

### Y lo de siempre

**Recordarle que tiene una pregunta importante que hacerme.** La tiene pendiente desde hace días y
quiere que se la recuerde; no sé cuál es.
