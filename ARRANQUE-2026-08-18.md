# Arranque · 18/08/2026

Retomamos Beckham. Lee `.spartax/log.md` (las entradas del 17/08, que son cinco bloques),
`state.json` y `.spartax/context.md` antes de proponer nada.

## Antes de nada

Auditoría por MCP de los tres workflows: `beckham_bot` (`nhOwpiGxikeU5DLR`), `beckham_generar_030`
(`OoJ2l7PmxSHLxXA4`) y `beckham_informe_mobility` (`Us5sFgXD9qVxJvxO`). `get_workflow_details` revienta
el límite de lectura y el harness guarda la respuesta en un fichero: se audita con jq o python desde
bash, sin gastar contexto. Y el MCP devuelve `credentials={}` en TODOS los nodos, también en los que
funcionan: si hay que comprobar una credencial, se ejecuta el workflow.

**`beckham_bot` ya NO está en `d15a8da8`. Ayer se tocó dos veces: ahora va por `004a90c7` o
posterior, con 54 nodos.**

## Dónde lo dejamos el 17/08

### Lo que se cerró y está probado

- **`WP-220`, el corpus fiscal al prompt.** Prompt **v9** publicado en LangSmith con tag `prod`,
  **59.708 caracteres** (v8 eran 46.878). Bloque fiscal nuevo de 12.826 caracteres entre
  `MODELOS (para tu referencia)` y `EJEMPLO 1`.
  **Pasó su gate 5 de 5 en conversación real**, con las dos trampas incluidas: soltó la salvedad
  literal del Modelo 721 y contestó «no lo tengo confirmado» al umbral del 720. Abrió con «Como
  información general» y no inventó ni una cifra.
- **`FechaLlamada`, probada de punta a punta.** Era la deuda que llevaba dos sesiones aplazándose.
  Ejecución `8115622`: `FechaLlamada=2026-08-22`, `MotivoCierre=Llamada agendada`. Los cuatro sitios
  confirmados con datos.
- **El expediente ya entra en el prompt desde Airtable.** Nodo nuevo `Leer_Expediente_Para_Prompt`
  entre `Formatear_conversacion1` y `Preparar_Prompt`, más `Preparar_Prompt` v3. El contexto pasa de
  **4 datos a 27**, más documentos, más lo ya decidido del caso, más el `ResumenBot` anterior.
- **`WP-10` cerrado** por decisión del usuario: es entorno de pruebas. `WP-228` y `WP-230`
  desbloqueados.
- **El tracker, al día.** 39 WPs: **11 cerrados, 28 abiertos, 51 puntos**. `map.html` y
  `ROADMAP-FASE2.md` regenerados. Camino crítico: 21 puntos, y su primer eslabón sin cerrar es
  **`WP-207`**, no `WP-220`.

### El bug de tres días que se cazó y se arregló

**Ni el informe ni el `.030` se generaban NUNCA desde el bot.** `Decidir_Status` escribía
`3. Pte hacer informe` y los dos workflows filtran por `{Status}="4. Informe enviado"`.
**297 ejecuciones de `beckham_informe_mobility` en tres días, todas verdes, todas de medio segundo,
cero informes.** El del 14/08 salió porque la fila se puso a mano en 4: aquellas tres ejecuciones
probaron el motor pero nunca el disparador. Arreglado: ahora escribe `4. Informe enviado`.

### Lo que sigue SIN probar, y es lo primero de hoy

**El disparador del informe.** Nadie ha visto todavía al bot generar un informe ni un `.030` por su
cuenta.

## Hoy toca, en este orden

1. **LA LLAMADA CON EL MANAGER.** Las seis preguntas están en su Slack, cada una con una propuesta
   para que solo tengan que decir sí: **M2** (dueño de leads), **M3** (base legal, opt-in y
   retención), **M6** (SLA y el texto literal que dice el bot), **el dueño de `WP-230`**, **M5**
   (vencida el 13/08, y es la única que cambia código ya escrito) y **el token de Calendly**.

2. **PROBAR EL DISPARADOR DEL INFORME.** 15 minutos. Tres celdas en `recIvWrCD6PcsE10p`: vaciar
   `SenalesComplejidad` y `MotivoCierre`, desmarcar `ConyugeQuiereAcogerse`, y **dejar `FechaLlamada`
   con el 22/08/2026**. Luego, en el chat: «ya hemos hablado con el fiscal, sigamos con los
   documentos» → subir papeles → cerrar sin dudas. Eso da `Expediente completo` → Status 4 → informe
   y `.030` en ≤15 min.
   **Es el único estado en el que el informe sale CON la fecha impresa**, así que prueba las dos
   cosas de golpe. Y ojo: al marcar `InformeListo` sale el correo al cliente.

3. **PROD.** Lo que lo bloquea: `WP-204` (el prompt niega dos tools que sí existen), `WP-216`
   (recolocar D/H/G/N), `T052` (idioma al canvas), limpiar las filas de test, y **el auth con token
   nuevo, que es lo último de todo**.

## Dos límites de diseño que hay que decidir, no programar

1. **El informe no puede imprimir NUNCA la fecha de la reunión.** `FechaLlamada` solo se rellena en
   caso complejo (Status 2) y el informe solo sale de un caso claro (Status 4). Son excluyentes, y
   está demostrado por comportamiento: con el contexto nuevo el bot **se niega** a seguir con los
   documentos de un caso complejo, y hace bien, porque es la regla del Bloque 6. Tres salidas:
   cambiar el flujo, meter Calendly, o quitar la línea del documento.

2. **Campo nuevo = CINCO sitios, no cuatro.** Tool, validador, mapeo del Upser, prompt **y el
   lector**. Si falta el quinto, el dato se guarda bien y **el bot lo vuelve a preguntar**. Así se
   descubrió ayer: el lector devuelve 21 claves y el escritor guarda 57.

## Lo que quedó a medias

- **El lector sigue cojo:** `Buscar Expediente en Airtable` lleva una whitelist de 21 campos en el
  propio nodo, y `Formatear Respuesta Expediente` devuelve esas 21. Ya está escrito el sustituto de
  48 claves (`docs/nodo-lector-expediente-2026-08-17.js`, probado con la fila real y 5 casos límite),
  pero **no se ha pegado** y hace falta además vaciar la whitelist del nodo. Ya no es urgente: el
  expediente entra por `Preparar_Prompt`.
- **La zona gris del `.030`: investigada y CERRADA sin cambios.** Llegó la muestra que faltaba, se
  probó un mapa alternativo de cinco campos de 3 alineados a la derecha, **y era falso**: las pruebas
  contra las 16 muestras lo cazaron al primer intento. El generador ya estaba bien. Queda documentado
  en el propio fichero para que nadie lo reintente. **12 de 14 muestras salen byte a byte.**
