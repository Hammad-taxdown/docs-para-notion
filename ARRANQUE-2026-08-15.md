/spartax Retomamos Beckham. Lee .spartax/log.md (las entradas del 14/08, que son muchas),
state.json y .spartax/context.md antes de proponer nada.

Dónde lo dejamos el 14:

- **WP-236 CERRADO Y EN PRODUCCIÓN.** El informe de memoria fiscal se genera solo, en PDF, en
  español y en inglés, con el logo de TaxDown, tipografía Times y el título centrado. Workflow
  `beckham_informe_mobility` (Us5sFgXD9qVxJvxO), activo, cada 15 minutos. El correo lo manda la
  automatización nativa de Airtable `wflZuMqIE5YYdnU8l`, encendida. Probado con cuatro ejecuciones
  reales y los correos recibidos. El PDF se construye a mano (objetos, xref, WinAnsiEncoding): no
  se incrusta nada y no hay librerías.
- **El nodo `Montar el informe` lleva 238.809 CARACTERES** (242.732 bytes: la diferencia son los
  acentos). El recuento SIEMPRE en caracteres, que es lo que cuenta el editor de n8n.
- **WP-235 sigue cerrado**, con los tres fallos del 14/08 arreglados: planta de dos caracteres,
  A1406 = '4', y `Vaciar Fichero030` antes de subir.
- **`FechaLlamada`**: columna creada y los cuatro sitios del bot puestos (parámetro 41 de la tool,
  línea 222 del validador, columna 57 del Upser, y el prompt v8). Sin probar en conversación real.
- **Prompt v8 publicado** (`docs/prompt-final-2026-08-14-v8.txt`, 46.878 caracteres). La traducción
  inglesa del informe está dada por buena por el usuario.
- **Once pruebas en verde.** `bash docs/montar-nodo-informe.sh` las pasa todas, no regenera si una
  se pone roja y revierte al COMPLETO anterior si la de integración falla.

**LO PRIMERO DE TODO, que quedó a medias ayer:** en `beckham_generar_030`
(OoJ2l7PmxSHLxXA4), nodo `Subir el fichero a Airtable`, **el campo URL sigue con `.first()`**
mientras el JSON ya tiene los dos `.item`. Hay que cambiar ese `.first()` por `.item`, borrando el
campo entero antes de pegar. Y después probarlo **con DOS filas pendientes de verdad**: es el único
modo de que el fallo se vea, y el 14/08 con `.first()` una fila perdió su fichero y la otra acabó
con dos copias del mismo.

Hoy toca, en este orden:

1. Cerrar el `.first()` de la URL del `.030` y probarlo con dos filas.
2. Probar `FechaLlamada` en una conversación real: decirle al bot una fecha de reunión, ver que
   llega a la columna, marcar `RegenerarInforme` y comprobar que el PDF la imprime en «Fecha de la
   reunión» en vez de «Por confirmar». Si llega vacía, el sitio que falla es el prompt.
3. **WP-220: el corpus fiscal al prompt.** Es lo que desbloquea el FAQ entero, que son 14 fichas.
   Ya está extraído en `docs/corpus-fiscal-beckham-2026-08-13.md` y el umbral no bloquea.
   Ojo: falta meter que la prestación por paternidad de la SS SÍ tributa, que la plantilla del
   informe ya lo dice y el prompt no.
4. Si queda hueco: que Fiscal revise la traducción inglesa del informe (va marcada en el código
   como no revisada), y los cuatro nombres de país que necesitan «del» ya están puestos.

Antes de nada, auditoría por MCP de los tres workflows: `beckham_bot` (nhOwpiGxikeU5DLR),
`beckham_generar_030` (OoJ2l7PmxSHLxXA4) y `beckham_informe_mobility` (Us5sFgXD9qVxJvxO).
Recuerda que `get_workflow_details` revienta el límite de lectura y el harness guarda la respuesta
en un fichero: se audita con jq/python desde bash, sin gastar contexto. Y que el MCP devuelve
`credentials={}` en TODOS los nodos, también en los que funcionan: si hay que comprobar una
credencial, se ejecuta el workflow.

Y tengo una pregunta importante que hacerte.
